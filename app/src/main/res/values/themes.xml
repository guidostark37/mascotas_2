<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mascotas_2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/my_light_primary</item>
    </style>

    <style name="Theme.Mascotas_2" parent="Base.Theme.Mascotas_2" />
    <style name="navegation" parent="Widget.Material3.BottomNavigationView">
        <item name="android:background">@color/nuevo</item>
        <item name="itemIconTint">@color/yellow</item>
        <item name="itemTextColor">@color/yellow</item>
    </style>
</resources>